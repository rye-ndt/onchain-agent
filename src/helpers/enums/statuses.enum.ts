export enum MATERIAL_STATUSES {
  DRAFT = "draft",
  PUBLISHED = "published",
  ARCHIVED = "archived",
  DELETED = "deleted",
}

export enum USER_STATUSES {
  ACTIVE = "active",
  BLOCKED = "blocked",
  DELETED = "deleted",
}
